创建一个前端项目，功能需求如下：
1. 在文件夹【第一轮线索】里面，有一张【第一轮线索集合.jpg】，你需要把它展示在网页当中，作为一个索引
2. 在这张图片上有 16 张卡片，你需要获取这些卡片的标题，即【第一轮线索 X】（X 是 1~16的数字）。并且获取每张卡片相对于图片左上角的偏移量。将它存下来
3. 给这张图片添加功能：当用户点击图片的特定区域的时候，结合之前存的偏移量信息，打开合适的图片，以走马灯的方式在网页中显示。点击返回还可以回到集合图片。
在此期间，不要发生网页跳转。
请你选择合适的技术选型实现如上的需求。尽量选择现成的前端框架和组件库。避免写过多的 css 和 html 代码。
每完成一步请向用户确认。


请重试 vite 构建的 react 项目。可以使用 pnpm 工具进行创建，这次不开启 rolldown

项目使用 fnm 进行管理 node 版本，本机安装了 node 22，请配置对应的文件以确保 使用 node 22 版本，并重试

现在的 App.jsx 似乎是适配移动端的竖屏。不要，请改成适配电脑端的横屏

图片的路径没有显示，请修改路径，很可能是 没有加 clues 文件夹。此外，这只是第一轮线索，请在资源文件里面文件夹设置注意这一点

hotspots 的位置是相较于图片的左上角，而非整个 main-image-container 的左上角。事实上，图片的长宽不一致，一定会比 main 要小。请据此修正。

还不错。不过图片比较宽，所以请修改 mock 的 hotspot，改成一个 2 * 8 的布局，而非 4*4 的布局

还不错，请你解析图片大概看一下分布地效果。上面那一行的左侧有一些 padding，下面那一行，在前 5 张图和后 3 张图中间有一些空间。请据此调整

oh，下面那一行的左边也有 padding，请也调一下

x = 4 有点多了，少一点；同时第二行的前五张图比第一行紧凑，请仔细再看一下图片然后调整坐标

挺好的。请整理我之前给你的所有用户输入，不要改变我的原文，不用包括你的回应。把它们整理成 prompts.txt 文件，导出到文件夹下。每一次输入之前用序号标注。包括我现在写的这一条。

把这个项目现有的功能写到 clue-viewer/README.md 下。